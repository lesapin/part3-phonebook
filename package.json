{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Exercises 3.1-3.10",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ./frontend/ && npm run build && cp -r dist ../",
    "deploy:full": "rm -rf dist && git add . && git commit -m uibuild && git push",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "./"
  },
  "author": "hurtsi",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.6.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
